# Set to "true" to enable testing mode: no API calls, form submit renders
# a fixed React snippet in the iframe to verify iframe rendering works.
# Copy this file to .env and set VITE_TESTING_MODE=true to use.
VITE_TESTING_MODE=false
