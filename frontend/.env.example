# Set to "true" for testing mode: no API calls. Visiting /app immediately
# renders a basic React + Tailwind + shadcn component in the iframe.
# Copy to .env and set VITE_TESTING_MODE=true to use.
VITE_TESTING_MODE=false
