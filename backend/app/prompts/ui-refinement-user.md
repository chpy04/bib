The user wants to refine the current dashboard component.

Layout hint: {layout_hint}

Available tasks (data props):
{verified_tasks_json}

Here is the conversation so far — all previous user requests are listed, and the current component code follows.

Previous user requests:
{chat_history_formatted}

Current component code:
```jsx
{current_code}
```

New refinement request: {refinement}

Apply ONLY the requested change to the current component. Keep everything else the same.
Return the complete updated GeneratedPage function.